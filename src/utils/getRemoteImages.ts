export const getTeamLogo = (id: string | number) => {
  return `https://media.api-sports.io/football/teams/${id}.png`;
};

export const getLeagueLogo = (id: string | number) => {
  return `https://media.api-sports.io/football/leagues/${id}.png`;
};
